# Environment variables / secrets
.env
.env.local
.env.*.local
.env.production
.env.development

# Vercel
.vercel

# Dependencies (if a package manager is introduced later)
node_modules/

# OS / editor cruft
.DS_Store
*.log
